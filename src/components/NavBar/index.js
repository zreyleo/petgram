import React from 'react';

export const NavBar = () => {
    return (
        <nav>
            <button>home</button>
            <button>favs</button>
            <button>user</button>
        </nav>
    )
}